{% set deletedIds = deletedFields|default([]) %}

{% for customField in form.customFields %}
    {% set customFieldEntity = customField.vars.data %}
    {% if customFieldEntity and customFieldEntity.getId() not in deletedIds %}
        {{ include('@CustomObjects/CustomObject/Form/Panel/' ~ customFieldEntity.getType() ~ '.html.twig', {
            customField: customField,
            customObject: customObject,
            panelId: panelId|default(null)
        }) }}
    {% endif %}
{% endfor %}

{% do form.customFields.setRendered() %}

